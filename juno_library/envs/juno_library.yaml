name: juno_library
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.9.*
  - git=2.40.*
  - setuptools=67.*
  - types-setuptools=67.*
  - pip>=23.*
  - pip:
    - -r ../../requirements.txt